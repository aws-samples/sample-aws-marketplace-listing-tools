AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: AWS Marketplace Seller Toolkit — SaaS integration tester and listing effectiveness scorer

Globals:
  Function:
    Timeout: 60
    MemorySize: 256
    Runtime: python3.12

Resources:

  # ── Lambda ──────────────────────────────────────────────────────────────────

  # CKV_AWS_116: DLQ not applicable — Lambda is invoked synchronously via API Gateway
  # CKV_AWS_117 / W89: VPC intentionally omitted — Lambda needs internet access for Marketplace APIs and Bedrock; VPC would require NAT Gateway adding cost with no security benefit
  ToolkitFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: handler.lambda_handler
      CodeUri: ../backend/
      ReservedConcurrentExecutions: 10
      Policies:
        - Version: "2012-10-17"
          Statement:
            # W11: Resource "*" required — Marketplace APIs, Bedrock, EventBridge, and CloudTrail
            # do not support resource-level permissions
            - Effect: Allow
              Action:
                - aws-marketplace:ResolveCustomer
                - aws-marketplace:GetEntitlements
                - aws-marketplace:MeterUsage
                - aws-marketplace:BatchMeterUsage
                - aws-marketplace:DescribeEntity
                - aws-marketplace:ListEntities
                - bedrock:InvokeModel
                - events:ListRules
                - cloudtrail:LookupEvents
                - sts:GetCallerIdentity
              Resource: "*"
            - Effect: Allow
              Action:
                - s3:PutObject
                - s3:GetObject
              Resource: !Sub "${FrontendBucket.Arn}/*"
      Events:
        Api:
          Type: HttpApi
          Properties:
            ApiId: !Ref Api
            Path: /test
            Method: POST

  # ── API Gateway ──────────────────────────────────────────────────────────────

  Api:
    Type: AWS::Serverless::HttpApi
    Properties:
      StageName: $default
      AccessLogSettings:
        DestinationArn: !GetAtt ApiAccessLogGroup.Arn
        Format: '{"requestId":"$context.requestId","ip":"$context.identity.sourceIp","requestTime":"$context.requestTime","httpMethod":"$context.httpMethod","path":"$context.path","status":"$context.status","protocol":"$context.protocol","responseLength":"$context.responseLength"}'
      CorsConfiguration:
        AllowOrigins:
          - "*"
        AllowMethods:
          - POST
          - OPTIONS
        AllowHeaders:
          - Content-Type

  ApiAccessLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub "/aws/apigateway/${AWS::StackName}"
      RetentionInDays: 30

  # ── Frontend S3 + CloudFront ─────────────────────────────────────────────────

  # CKV_AWS_18 / W35: Access logging intentionally omitted for this sample tool
  # CKV_AWS_21: Versioning intentionally omitted for this sample tool — bucket contains only a single generated index.html
  FrontendBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true

  FrontendBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref FrontendBucket
      PolicyDocument:
        Statement:
          - Effect: Allow
            Principal:
              Service: cloudfront.amazonaws.com
            Action: s3:GetObject
            Resource: !Sub "${FrontendBucket.Arn}/*"
            Condition:
              StringEquals:
                AWS:SourceArn: !Sub "arn:aws:cloudfront::${AWS::AccountId}:distribution/${CloudFrontDistribution}"

  CloudFrontOAC:
    Type: AWS::CloudFront::OriginAccessControl
    Properties:
      OriginAccessControlConfig:
        Name: !Sub "${AWS::StackName}-oac"
        OriginAccessControlOriginType: s3
        SigningBehavior: always
        SigningProtocol: sigv4

  # CKV_AWS_86 / W10: CloudFront access logging intentionally omitted for this sample tool
  # CKV_AWS_68: WAF intentionally omitted for this sample tool — adds cost with no benefit for a static frontend
  CloudFrontDistribution:
    Type: AWS::CloudFront::Distribution
    Properties:
      DistributionConfig:
        Enabled: true
        DefaultRootObject: index.html
        Origins:
          - Id: S3Origin
            DomainName: !GetAtt FrontendBucket.RegionalDomainName
            OriginAccessControlId: !Ref CloudFrontOAC
            S3OriginConfig: {}
        DefaultCacheBehavior:
          TargetOriginId: S3Origin
          ViewerProtocolPolicy: redirect-to-https
          CachePolicyId: 4135ea2d-6df8-44a3-9df3-4b5a84be39ad  # CachingDisabled
        ViewerCertificate:
          CloudFrontDefaultCertificate: true
          MinimumProtocolVersion: TLSv1.2_2021

  # ── Custom Resource: Deploy Frontend ───────────────────────────────────────
  # Uses the main Lambda to read index.html from its own package and upload to S3

  FrontendDeploy:
    Type: Custom::FrontendDeploy
    DependsOn:
      - FrontendBucketPolicy
    Properties:
      ServiceToken: !GetAtt ToolkitFunction.Arn
      DestBucket: !Ref FrontendBucket
      ApiEndpoint: !Sub "https://${Api}.execute-api.${AWS::Region}.amazonaws.com/test"

Outputs:
  TestToolUrl:
    Description: Open this URL to use the AWS Marketplace Seller Toolkit
    Value: !Sub "https://${CloudFrontDistribution.DomainName}"

  ApiEndpoint:
    Description: API endpoint (pre-configured in the tool)
    Value: !Sub "https://${Api}.execute-api.${AWS::Region}.amazonaws.com/test"

  FrontendBucket:
    Description: S3 bucket for frontend assets
    Value: !Ref FrontendBucket
